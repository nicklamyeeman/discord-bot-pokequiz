const SECOND = 1000;
const MINUTE = SECOND * 60;
const HOUR = MINUTE * 60;
const DAY = HOUR * 24;

const TIME_BETWEEN_RUSH_CLAIMS = 2 * HOUR;

module.exports = {
  SECOND,
  MINUTE,
  HOUR,
  DAY,
  TIME_BETWEEN_RUSH_CLAIMS,
};
